export enum AuthForm {
  LOGIN = 'login',
  REGISTER = 'register',
}

export enum authMessageLinkProps {
  REGISTER_TITLE = 'Register',
  REGISTER_LINK = 'register',
  REGISTER_MESSAGE = "Don't have an account?",
  LOGIN_TITLE = 'Login',
  LOGIN_LINK = 'login',
  LOGIN_MESSAGE = 'Already have an account?',
}
