import React from 'react';
import { AccountContainer } from '@/components';

export const Listings = () => {
  return (
    <AccountContainer>
      <div>listing</div>
    </AccountContainer>
  );
};
