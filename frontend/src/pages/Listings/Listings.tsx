import { AccountContainer } from '@/components';
import { ListingsList } from '@/pages';

export const Listings = () => {
  return (
    <AccountContainer>
      <ListingsList />
    </AccountContainer>
  );
};
