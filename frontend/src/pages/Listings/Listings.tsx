import { AccountContainer } from '@/components';
import { ListingsContainer } from '@/pages';

export const Listings = () => {
  return (
    <AccountContainer>
      <ListingsContainer />
    </AccountContainer>
  );
};
